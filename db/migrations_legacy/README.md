This directory contains the pre-squash migration history that was previously under `db/migrations/`.

The active migration runner embeds and applies only `db/migrations/*.sql` (currently a single squashed baseline).

