# Worker environment updates
# Copy this to .env.workers and customize
# DO NOT commit .env.workers (it's in .gitignore)
# Usage: ./scripts/deploy-workers.sh --env-file .env.workers

# Logging
LOG_LEVEL=debug
# LOG_FORMAT=json

# Worker tuning
# WORKER_CONCURRENCY=4
# WORKER_JOB_TIMEOUT=10m
