services:
  metabase:
    image: metabase/metabase:latest
    environment:
      # Persist Metabase's internal application DB (H2) to a named volume.
      MB_DB_FILE: /metabase-data/metabase.db
      JAVA_TIMEZONE: ${TZ:-UTC}
    ports:
      # Bind Metabase to localhost by default; set TAILSCALE_BIND_IP to expose over Tailscale.
      - "${TAILSCALE_BIND_IP:-127.0.0.1}:${METABASE_PORT:-3000}:3000"
    volumes:
      - metabase_data:/metabase-data
    networks:
      - google_flights_master_default
    restart: unless-stopped

volumes:
  metabase_data:

networks:
  # Attach to the existing Flight API stack network so Metabase can reach Postgres as `postgres`.
  google_flights_master_default:
    external: true
